/**
 * OpenAPI route table — medications. Barrel preserving the original module path.
 *
 * Part of the OpenAPI route table; aggregated in `../index.ts`.
 */
export { medicationResource } from "./schemas";
export { medicationPaths } from "./paths";
