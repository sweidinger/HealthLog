# v1.4.39 QA — Simplification Findings

Reviewed diff: `git diff v1.4.38.8..develop`. Focus on dead helpers, near-duplicate code, and prunable fallbacks introduced by the W-MOOD / W-MED / W-SUM / W-WMY / W-SINCE waves.

No automated edits were applied — every candidate below is either consumed by tests as a contract anchor, kept around as an intentional v1.5 hook, or non-trivial enough to warrant Marc's sign-off before deletion.

## Suggested removals
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-cumulative.ts:54` — `isCumulativeType` — exported, tested, but no production caller; `/api/dashboard/summary/route.ts` reads `CUMULATIVE_HK_TYPES` directly and `/api/analytics/route.ts` uses `isCumulativeDaySumType` from `cumulative-day-sum.ts`. The shape was added "for future routes" by W-SUM but never wired — confidence: likely.
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-cumulative.ts:83` — `readCumulativeDaySums` — exported and tested but no production caller. The cumulative tile flow inlines `sum_value` off `SparklineRow` instead — confidence: likely.
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-cumulative.ts:121` — `readCumulativeDaySumsBatch` — exported and tested but no production caller. The W-SUM phase-report frames this as the future analytics-route batch reader; today `/api/analytics` still routes through `pickCumulativeDaySum` on live measurements — confidence: likely.
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-cumulative.ts:164` — `resolveBucketSum` — exported and tested but no production caller; the inline `sum_value ?? mean * count` fallback in `dashboard/summary/route.ts` reimplements the same shape. Once a caller exists this should be the single source of truth, but right now it is dead — confidence: likely.
- `/Users/marc/Projects/HealthLog/src/lib/analytics/summaries-slice.ts:615` — `computeLongWindowSummary` — only the file's own test suite imports it. The phase report flags it as the v1.5 multi-year-card hook; until that card lands the function ships dead in the production bundle — confidence: certain (no callers in `src/`).
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-wmy.ts:125,137,149` — `readWeekRollups` / `readMonthRollups` / `readYearRollups` — only the wmy reader's own tests import them; the production `readBestGranularityRollups` walker uses the internal `readGranularity`. Either delete the three thin wrappers and inline the call into the tests, or downgrade them to non-exported helpers — confidence: certain.
- `/Users/marc/Projects/HealthLog/src/lib/medications/compliance-rollups.ts:453` — `ensureUserMedicationComplianceFresh` — the only caller is its own JSDoc reference. The coverage-probe → boot-backfill cascade fully owns the cold-mount path; this "kept for symmetry" wrapper is dead today — confidence: certain.

## Suggested dedups
- `/Users/marc/Projects/HealthLog/src/lib/mood/rollups.ts:571` — `startOfUtcDay` — byte-identical to `/Users/marc/Projects/HealthLog/src/lib/measurements/rollups.ts:524`. Suggested action: lift to a shared `src/lib/time/start-of-utc-day.ts` (or re-export from `tz/format`) and have both rollups files import from it. The Postgres `date_trunc` parity comment stays meaningful in one place.
- `/Users/marc/Projects/HealthLog/src/lib/mood/rollups.ts:583` — `bucketSpan` — structurally identical to `/Users/marc/Projects/HealthLog/src/lib/measurements/rollups.ts:537` (only the variable name differs: `measuredAt` vs `moodLoggedAt`). Suggested action: extract a generic `bucketSpanForUtcInstant(instant: Date, granularity: RollupGranularity)` and share it. WEEK / MONTH / YEAR branches are line-by-line clones.
- `/Users/marc/Projects/HealthLog/src/lib/medications/compliance-rollups.ts:73` — `wallClockInTz` — third copy of the same helper alongside `src/lib/medications/scheduling/cadence.ts:107` and `src/lib/tz/format.ts` (private). Suggested action: export the canonical one from `lib/tz/format.ts` and delete the two private copies. Marc-Voice rationale: "one tz helper, one bug fix radius."
- `/Users/marc/Projects/HealthLog/src/lib/medications/compliance-rollups.ts:96` — `tzOffsetMinutes` — third copy alongside `src/lib/tz/format.ts:225` and `src/lib/medications/scheduling/cadence.ts:149`; `src/lib/measurements/drain-per-sample-cumulative.ts:126` has a fourth variant (`tzOffsetMinutesAt`). Suggested action: keep the `tz/format.ts` implementation and have the other three import it.
- `/Users/marc/Projects/HealthLog/src/lib/medications/compliance-rollups.ts:126` — `startOfDayUtcInTz` — bespoke two-pass convergence. `lib/tz/format.ts` already exports `userDayKey`; a `startOfUserDay(dayKey, tz)` companion would let this file shed its private DST helper.
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-wmy.ts:204` — `aggregateWmyBuckets` — near-duplicate of `src/lib/measurements/rollup-read.ts:59` `aggregateBuckets`. The new version is a strict superset (adds `sum`). Suggested action: extend `aggregateBuckets` to carry `sum`/`sumValue` and delete `aggregateWmyBuckets`; or delete `aggregateBuckets` and have its single caller in `rollup-read.ts` consume the WMY one. Keeping two near-identical reducers invites a future drift bug.

## Suggested simplifications
- `/Users/marc/Projects/HealthLog/src/lib/medications/compliance-rollups.ts:177` — `recomputeMedicationComplianceForDay` re-derives `nextDayKey` via an IIFE that builds a probe-`Date`, calls `wallClockInTz`, and re-pads its components. Once `startOfDayUtcInTz` accepts a "next day" mode (or via a `+1 day` helper) this 8-line block collapses to one call. Behaviour-preserving.
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollups.ts:751` — boot-backfill discovery `$queryRaw` now `UNION`s the per-type-missing branch with the legacy-NULL `sum_value` branch. Once Marc's tenant has converged (per the W-SUM rollout plan), the legacy-NULL branch becomes a permanent no-op walk over the full rollup table. Suggested action: schedule the `OR r2."sum_value" IS NULL` branch for removal in v1.4.40 + add a TODO with the cut-off date — keeps the discovery query single-table after convergence.
- `/Users/marc/Projects/HealthLog/src/app/api/analytics/route.ts:241` — `ANALYTICS_LIVE_WINDOW_DAYS = 90` and its 30-line provenance comment live inside `buildAnalyticsResponse`. Hoist to a module-level constant alongside the other thresholds so a future tuner does not have to scroll into the handler body. Behaviour-preserving.
- `/Users/marc/Projects/HealthLog/src/app/api/dashboard/summary/route.ts:441` — the cumulative-vs-spot `useSum` branch is a nested ternary `useSum ? (row.sum_value !== null ? Number(row.sum_value) : Number(row.mean) * Number(row.count)) : Number(row.mean)`. Per CLAUDE.md "no nested ternaries"; lift the inner branch into a named local: `const cumulativePoint = row.sum_value !== null ? Number(row.sum_value) : Number(row.mean) * Number(row.count); const point = useSum ? cumulativePoint : Number(row.mean);`. Or call the (currently dead) `resolveBucketSum` once it is wired.
- `/Users/marc/Projects/HealthLog/src/lib/measurements/rollup-read-cumulative.ts` — once the four exports are either wired or removed, the file plus its 195-line test file collapses by ~360 lines. The W-SUM phase-report's "ready-for-v1.5" framing should be reflected by deletion-with-revival-PR rather than dead exports living in `develop`.

## Applied
None — every candidate above either (a) intersects test contracts that should be migrated together, (b) sits behind a v1.5 wiring intent Marc signed off on in the phase reports, or (c) is a cross-file dedup whose target module needs design input. Listing as suggestions keeps Marc's voice in the eventual commit message.
