# v1.7.0 marathon — state anchor (2026-05-31, FULLY AUTONOMOUS)

Maintainer OFFLINE / away from machine. FULL autonomy, no questions — resolve
everything self. Bar = "award-worthy", ship it end-to-end. If the session
compacts, RESUME from this file. git commits are persistent in the repo (not in
context) — only the plan lives here.

## Mission
Finish v1.7.0 and ROLL IT OUT autonomously: integrate remaining work → final gate
(incl. e2e) → squash-merge PR #219 release/v1.7.0→main → tag v1.7.0 → docker-publish
→ Coolify force-redeploy apps01 → verify /api/version==1.7.0 + AASA → GH release →
ios-coord final reply → memory → PushNotification to Marc. Maintainer authorised the
tag+deploy himself (overrides the usual "never tag" rule).

## Working base
- Branch `release/v1.7.0` (LOCAL is the truth; ~66 commits since main incl. README
  8ff7776). origin/release/v1.7.0 may lag at 424d12de — PUSH local before any NEW
  worktree agent (they branch from origin/HEAD = release/v1.7.0). origin/HEAD already
  `git remote set-head origin release/v1.7.0`.
- PR #219 (Draft) release/v1.7.0 → main already open.
- package.json + openapi.yaml already at 1.7.0; CHANGELOG has the v1.7.0 entry
  (UPDATE it before tag to add W17 mood/intake sync, W18 snapshot seed, W19 widget
  round-trip, W23 mood-reminder fix, W24 mood-hour; + W15/W16 if notable).

## macOS worktree trap (HIT TWICE — guard hard)
Before EVERY cherry-pick / commit on the main tree: `git branch --show-current`
must == release/v1.7.0 (a worktree agent once switched the main tree to its branch).

## Migrations allocated: 0091–0097 used. W24 → 0098 IF it needs one (prefer existing
notification-prefs JSON to avoid a migration).

## DONE + INTEGRATED on release/v1.7.0 (verified green, 5856 unit tests at W19)
W1–W14 (all features) + reconcile A/B/C/D (22 commits, 3 High + Mediums) + FHIR
LOINC byte-align + W15 drain-dedup + W16 a11y roving-tabindex + W17 mood/intake sync
(mig 0097, multi-domain cursor) + W18 snapshot metricStates+27-id + W19 widget
round-trip (27-id persist) + README 8ff7776.

## IN FLIGHT (background agents)
- W23 mood-reminder double-opt-in fix (agent ad72d47895ec80beb, non-worktree main tree).
- W20 docs-site update (agent aded9484e68b30d1e, repo /Users/marc/Projects/healthlog-docs).

## REMAINING TODO (autonomous)
1. W23 returns → it commits on release/v1.7.0 (verify branch). Then W24 (per-user
   mood-reminder hour, non-worktree, after W23 — same notification/cron files;
   prefer no migration, else 0098). Default 22:00 back-compat.
2. After ALL code integrated: `pnpm prisma generate` (W17 cols) → FINAL GATE:
   typecheck + lint + `pnpm test` + `pnpm e2e` (Playwright — catches release reds) +
   openapi:generate + openapi:check. Fix any red BEFORE tag.
3. Update CHANGELOG + PR #219 body (remove the deferred-items that got done: drain
   refactor + a11y; they're now IN).
4. Squash-merge PR #219 → main. Tag v1.7.0, push tag → docker-publish.yml.
5. Coolify force-redeploy on apps01 (auto-deploy lags) via mcp__coolify-apps01__*
   (app uuid pg8wggwogo8c4gc4ks0kk4ss). Wait healthy.
6. Verify /api/version == 1.7.0 live; verify AASA /.well-known/apple-app-site-association
   on the default host (iOS said non-blocking but confirm + report).
7. GH release notes (from CHANGELOG, Marc-voice, no AI/agent vocab, no PII).
8. ios-coord FINAL reply file: confirm scheduleType on every schedule row in YAML,
   snapshot seed (metricStates+27-id) shipped, widget round-trip shipped, mood-reminder
   fixed, mood-hour added, AASA status. INCLUDE an explicit note that BOTH sides read
   .planning/ios-coord/ automatically on change (Marc's instruction) — it's the
   standing comms channel.
9. Memory update (project_v170_marathon.md) → LIVE.
10. PushNotification to Marc: v1.7.0 live + verified.

## DOCS TRACK (separate repo, does NOT block app tag)
- W20 docs-site update in flight. Then W21 (human-readable API + sync-contract page:
  domains measurements/mood/intakes, identity keys externalId vs server id, values,
  tombstones, opaque cursor, conflict rules, what iOS sends/receives, auth errorCodes).
- Then W22 (explanation-completeness per the "explain the WHY" doctrine + SEO/findability
  + maintainability — read-only audit then polish).
- APPLY .planning/v1.7.0-marathon/docs-maintainer-notes.md at docs review: Gravatar→
  self-hosted MIGRATION STORY (privacy), and add "why" rationale at critical points.
- Docs commits stay local until built green; push + deploy docs-site separately.

## ios-coord exchange (standing channel, monitor running)
Incoming iOS files this session: reply, offline-sync-answers, convergence-locks,
pin-and-snapshot, mood-reminder. Reply by writing a file back. Both sides auto-read
on change.

## RESUME POINT (latest — post-compaction continue here)
DOMAIN = `healthlog.bombeck.io` (apps01, app uuid pg8wggwogo8c4gc4ks0kk4ss).
TAGGED v1.7.0, merged to main `50e0a652`, docker-publish DONE, Coolify redeploy
queued (deploy `tkn19ny9jz6qqhf2f01m2bxp`), docs pushed (`8fd69ea`), GH release DONE.
Background poll `b93nr1znr` watches https://healthlog.bombeck.io/api/version for 1.7.0 + AASA.
STILL TODO after live-verify: (1) ios-coord final reply file — server mood/intake sync
is COMPLETE (NOT deferred, corrects earlier note); scheduleType/asNeeded/cycle*/nextDueAt/
due+expectedCount shipped; 27-id widget round-trip (iOS can drop byMergingIosOnlyDefaults);
snapshot metricStates+layoutCatalogue seed; mood-reminder card-alone fix + per-user
notificationPrefs.mood.reminderHour; AASA status; note both sides auto-read the dir.
(2) update memory project_v170_marathon.md to LIVE. (3) PushNotification to Marc.
---
## v1.7.0 SHIPPED LIVE on apps01 (healthlog.bombeck.io) + demo (demo.healthlog.dev). Docs+GH+iOS done.

## v1.7.1 IN FLIGHT (card-fix — maintainer-found gap)
v1.7.0's medication-card overhaul only touched the DETAIL page header
(medication-detail-header.tsx = Pencil/History/SlidersHorizontal). The OVERVIEW
CARD (medication-card.tsx) still has ONLY the Pencil. Maintainer wants the same 3
actions (edit/history/advanced) on the overview card too. Fix agent
`a9e1311032c5c581f` on branch `fix/v171-medication-card-actions` (off main).
WHEN AGENT RETURNS: (1) verify branch, gate typecheck/lint/test; (2) bump
package.json → 1.7.1 + CHANGELOG + openapi regen; (3) merge branch → main, tag
v1.7.1, push tag (docker-publish); (4) redeploy BOTH:
 - apps01: mcp__coolify-apps01__deploy pg8wggwogo8c4gc4ks0kk4ss force;
 - demo (edge01, MCP unreachable — use SSH): `ssh root@46.225.114.153 "cd /data/coolify/applications/ck8cs4osswg8w440gskw08w8 && docker compose pull && docker compose up -d"`;
(5) verify /api/version==1.7.1 on BOTH healthlog.bombeck.io + demo.healthlog.dev;
(6) PushNotification to Marc.

ALL code (W1–W25) + docs (W20/W21/W22) DONE. release/v1.7.0 pushed @ 718468e9
(CHANGELOG+openapi finalized, dead-code DashboardIosOnlyWidgetId type removed).
Local gate GREEN: typecheck/lint/test 5878/openapi all pass. Docs repo
(healthlog-docs main) has 8 unpushed commits, build green, NOT pushed yet.
NEXT (autonomous, Marc authorised tag+deploy himself, no questions):
1. WAIT CI on PR #219 (`gh pr checks 219`) — e2e/lint/test/integration/builds.
   First run had dead-code fail (now fixed); re-running on 718468e9.
2. CI green → `gh pr merge 219 --squash --delete-branch` → main.
3. `git checkout main && git pull` → `git tag v1.7.0 && git push origin v1.7.0`
   → triggers docker-publish.yml.
4. Coolify force-redeploy apps01 app uuid pg8wggwogo8c4gc4ks0kk4ss
   (mcp__coolify-apps01__deploy / control). Wait healthy.
5. Verify /api/version == 1.7.0 live + AASA /.well-known/apple-app-site-association.
6. Docs: `git -C ~/Projects/healthlog-docs push` (main) → docs deploy → verify live.
7. GH release v1.7.0 from CHANGELOG (Marc voice, no AI vocab, no PII).
8. ios-coord final reply file (scheduleType confirmed, snapshot seed shipped, widget
   round-trip shipped, mood fixes, mood server-side COMPLETE not deferred, AASA) +
   note both sides auto-read the dir.
9. Update memory project_v170_marathon.md → LIVE.
10. PushNotification to Marc: v1.7.0 live + verified.
