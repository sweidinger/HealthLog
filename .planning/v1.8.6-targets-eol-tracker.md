# Targets (Zielwerte) page EOL tracker

- `/targets` deprecated in v1.8.6 — target-range editing moved inline into Insights (per-metric reference panel mounts `<TargetEditSheet>`); page dropped from nav, kept routable via the `/zielwerte`→`/targets` proxy redirect; remove candidate after ~10 releases (UI-only removal — `GET /api/insights/targets`, `PUT/DELETE /api/user/thresholds`, and `src/components/targets/*` all stay because Insights consumes them).
