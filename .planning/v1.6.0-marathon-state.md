# v1.6.0 marathon — state anchor (2026-05-29)

Maintainer offline, full autonomy, token budget irrelevant, bar = "very clean
functionality" + production-ready across **app + docs + landing + GitHub + SEO**.

## Working base
- Branch: `release/v1.6.0` off `main` @ `d01d1d1d` (main had 3 "Update README.md"
  web commits past the v1.5.6 tag `8528c3b4`).
- Drive the medication overhaul MYSELF in the main tree (macOS case-insensitive
  worktree misroute risk). Separable streams (docs/landing/SEO/README) → agents
  in their OWN repos.

## CI is RED on main since v1.5.6 (must all go green)
- **Knip**: dead `src/components/medications/todays-dose-card.tsx` + unused export
  `DoseUnit` in `src/lib/ai/coach/medication-extract-prompt.ts`. (Resolve in med build.)
- **Security & Quality**: OpenAPI drift — committed `docs/api/openapi.yaml` says
  `version: 1.5.5`, regen yields `1.5.6` (v1.5.6 forgot to regen). Trivial: regen+commit.
- **e2e**: 14 failed = ALL medication-wizard specs (daily/weekly/biweekly/monthly/
  rolling/oneshot/compose × desktop+mobile) — broken by v1.5.6 detail rewrite.
  Must rewrite specs to the NEW editor flow.

## Open issues / PRs
- #218 safeFetch wrapper + #217 DNS-rebinding IP pin → v1.5.6 finished egress
  migration w/ `requirePublicHost`. VERIFY vs `src/lib/safe-fetch.ts`; close or finish gap.
- PR #192 bump react + @types/react (React pinned EXACT 19.2.5 — evaluate carefully).
- PR #182 bump actions/download-artifact 4→8 (CI action; likely safe).

## Medication overhaul — design (APPROVED by maintainer)
Decisions: (a) wide DIALOG not route; (b) injection-site body-map picker shown for
injections but COLLAPSIBLE/optional.
1. ONE editor: merge 8-step wizard + AdvancedSettingsSheet. Desktop lg/xl = wide
   (~max-w-4xl) 2-col scrollable sectioned form (Identity / Plan / Reminders /
   Specific). Mobile = bottom-sheet grouped sections (accordion), fix min-h empty.
   Kills the 3-4-tap nested path.
2. Detail page + card actionable: restore card REWIND/history icon (1 tap →
   intake history) + direct edit pencil (opens editor, not picker). Make
   CadenceSummaryRow editable again (remove dead `hideEdit` at [id]/page.tsx:290).
   Replace `Edit ▾` two-option picker (medication-detail-header.tsx:125-138) with a
   single "Bearbeiten". Bring SideEffects + Scheduling sections back onto detail page.
3. Restore lost fields into editor: GLP-1 once-weekly preset (weekday pills +
   suggest-last-pick), `dosesPerUnit`, independent category vs treatmentClass,
   per-schedule dose/label.
4. One-Time-Injection: NEW additive enum `deliveryForm (ORAL|INJECTION|…)` on
   Medication, DECOUPLED from treatmentClass. one-time injection = oneShot=true +
   deliveryForm=INJECTION. Injection-site picker available when INJECTION (not only
   GLP1). Migration backfills ORAL. Schema already has `oneShot` + INJECTION_* enums.
5. Restore API token multi-language code examples (cURL/wget/JS/PowerShell) removed
   in commit `ef21967d` (collateral of lifting IntakeImportDialog). i18n keys still
   exist (`medications.requestExample`, etc.). Port into api-tokens-row.tsx; wire
   minted token into templates; anglicise old German placeholders (DEIN_TOKEN).

## Pre-wizard baseline for archaeology = commit `e74eff29` (parent of 1757b353).
Old single dialog `medication-form.tsx` (1162 LOC) had everything inline. Lost:
GLP1 weekly preset, dosesPerUnit, category/class split, per-schedule dose/label,
6-item footer menu (import/API/phase/pause/notify/purge/delete), Reset action.
SideEffectsSection + SchedulingSection still live on legacy `/[id]/history` route.

## Responsive root cause
`ResponsiveSheet` (src/components/ui/responsive-sheet.tsx) flips at 768px
(use-is-mobile.ts:40). Desktop branch hardcodes `sm:max-w-md` (448px). Consumers:
AdvancedSettingsSheet `sm:max-w-lg` (512), wizard `sm:max-w-[560px]`. No lg/xl width.

## Background agents running (read-only, write to .planning/site-audit-2026-05-29/)
- frontend-arch.md — site-wide non-medication frontend/architect audit (agent ac6e6d0cfa556d5ca)
- admin-settings.md — admin IA / duplicates / dead / hallucinated (agent a9347de544e71e40e)
- dead-code-structure.md — dead code + maintainability + codemap (agent ad71613263c7fa273)
Consume these when they land; create fix-tasks.

## Other repos
- docs: `~/Projects/healthlog-docs` — Astro Starlight. ON BRANCH
  `docs/apns-pinning-selfhost` (in-progress, not main) — check before editing.
- landing: `~/Projects/healthlog-landing` — Next.js, main, untracked `.notes/`.
- `-audit-fixes` siblings = stale v1.3.3 branches. `healthlog-marketing` = not a git repo.
- Screenshots: Desktop EMPTY. `~/Downloads` has mood imgs (1-Lausig..5-Super gut.png,
  mood.jpg, May 24) — UNCONFIRMED if these are the "new" ones. Clarify w/ maintainer.

## Voice rules (every committed artefact, all repos)
English; maintainer voice (present-tense imperative commits, factual prose); NO
AI/agent/wave/phase/marathon vocabulary; no personal names; no real health figures.
Co-Authored-By: Claude Opus 4.7 (1M context) trailer is the only exception.
Docs/landing must read as human-written, natural, no hallucinated features, add
explanatory links (Wikipedia etc.). SEO push (dual agent) over docs+landing,
push iOS + HealthKit strengths.

## Task list (durable): #7 detail-edit (in_progress), #8 responsive editor,
#9 restore fields, #10 one-time-injection, #11 token examples, #12 QA+gates,
#13 site-wide audit act, #14 green CI, #15 issues #217/#218, #16 dependabot,
#17 docs, #18 landing, #19 README/repo, #20 SEO.

## CRITICAL finding (dead-code agent, DONE — see dead-code-structure.md)
- **v1.5.1 read-flip never landed.** Canonical engine `src/lib/medications/scheduling/
  recurrence.ts` has ONE caller (reminder worker). Dashboard-summary + intake-today
  still use legacy walker `expandTodayIntakes` (`src/lib/medication-schedule.ts`),
  which ignores `rrule` + skips `intervalWeeks>1` (self-documented :132-138). Feeds
  `project-today-intakes.ts:31` → `/api/medications/intake?scope=today` +
  `/api/dashboard/summary` (route.ts:350). LIVE DIVERGENCE on bi-weekly/RRULE/rolling.
  Fix = route today-projectors through canonical engine. `daysOfWeek` column drop
  UNSAFE until then; keep `legacy-bridge.ts` dual-write. → fold into med overhaul.
- Structure: `src/lib/jobs/reminder-worker.ts` (2454 LOC) god-dispatcher ~30 handlers
  → continue the existing extract-to-sibling pattern, thin wiring shell. (P2.)
- knip: 2 items both in medications/** (todays-dose-card.tsx, DoseUnit) → resolve in build.

## ALL 3 app audits DONE (files in .planning/site-audit-2026-05-29/):
- admin-settings.md: moodLogGlobal setting has NO UI toggle (add to ServicesSection);
  `/api/admin/status-overview` orphaned (~230 LOC, delete/wire); host-metrics:135-143
  comment promises a memTotal tooltip that doesn't exist; duplicate useVersion/
  useOfflineGeoState hooks (consolidate to _shared.tsx); 3 AI sections share Sparkles
  icon + integrations uses AlertTriangle (distinct icons). requireAdmin gates all routes (clean).
- frontend-arch.md P1: (1) dashboard range colors text-green/orange/red-400 bypass
  semantic --success/--warning/--info tokens → fail light AA (page.tsx:126-128,145/151/158
  + targets/page.tsx:241); (2) togglePrivacy swallows failures, no res.ok check, fires
  callback unconditionally → silent privacy desync (ai-section.tsx:1797-1805);
  (3) MAX_COMMENT_LENGTH=25 too short for clinical notes (measurement-form.tsx:28 +
  measurement-list.tsx:108, check Zod + DB). P2: ResponsiveSheet cramp, GLP-1 chips
  for all users (mood-form.tsx:255-301), 1514-LOC dashboard client component.

## Versioning: multi-feature UX milestone → v1.6.0 (minor) justified per semver memory.
End: green all CI → squash-merge to main → tag v1.6.0 → docker-publish → Coolify
deploy → verify /api/version == 1.6.0 live → GH release → memory.

## SESSION PROGRESS (2026-05-29, post-compaction continuation)
- **#11 DONE + committed** on `release/v1.6.0`: multi-language request snippets
  (cURL/wget/JS-fetch/PowerShell) restored into `api-tokens-row.tsx`. Minted token
  interpolates live (`mintedToken ?? "YOUR_TOKEN"`); idempotency prefix anglicised
  to `intake-`. Uses existing key `medications.requestExample`. typecheck+tests green.
- **Docs/landing/README/SEO audit DONE** → `.planning/site-audit-2026-05-29/docs-landing-readme-seo.md`.
  P1s: iOS/HealthKit nearly invisible on landing+docs (no iOS section/TestFlight/screenshots);
  landing `layout.tsx` JSON-LD `softwareVersion` still 1.4.32 (→1.6.0); README `GET /api/auth/me`
  still claims Gravatar URL (REMOVED v1.5.5 → now self-hosted avatars, stated backwards);
  docs missing sitemap (`@astrojs/sitemap` not installed). Screenshot `…22.03.55…` is a DEV
  TERMINAL → DO NOT PUBLISH. Cross-surface drift: achievements 30+/59, Node 20/22, version.
- **#10 deliveryForm — SCHEMA+MIGRATION DONE + committed.** `MedicationDeliveryForm`
  enum (ORAL|INJECTION|OTHER) + `deliveryForm @default(ORAL)` column on Medication; migration
  `0088_v160_delivery_form` (CREATE TYPE + ADD COLUMN default ORAL). Prisma client regen'd,
  typecheck green. **REMAINING for #10:** (3) `src/lib/validations/medication.ts`
  add `MEDICATION_DELIVERY_FORM_VALUES`/type + `deliveryForm: z.enum(...).optional()` on BOTH
  create+update schemas. (4) POST `route.ts:182`-style field-by-field `...(deliveryForm !== undefined && { deliveryForm })`.
  (5) PUT `[id]/route.ts` — `candidate` object built ~line 181-182 area, add same line; `data: candidate`
  at line 261. (6) detail GET must RETURN `deliveryForm` (check `[id]/route.ts` GET select/include —
  it does `include:{schedules:true}` at line 32 so scalar deliveryForm flows automatically; VERIFY
  the snapshot interface in `[id]/page.tsx` + add `deliveryForm` to `MedicationDetailSnapshot`).
  (7) regen openapi LATER (batched in #14). UI to SET deliveryForm = part of editor build (#8/#9).
- **Then #7 detail page**: remove dead `hideEdit` ([id]/page.tsx:289-293 → pass real onEdit→setWizardOpen),
  replace 2-option Bearbeiten dropdown (medication-detail-header.tsx:112-139) with single button →
  onEditPlan, restore card rewind/history icon + direct edit on `medication-card.tsx`, bring
  SideEffectsSection + SchedulingSection back onto detail (they live at `src/components/medications/
  {SideEffectsSection,SchedulingSection}.tsx`, currently only on legacy `/[id]/history`).
- **Then #8/#9 EDITOR (biggest)**: wide desktop sectioned form (~max-w-4xl, 2-col scroll) reusing the
  PURE helpers in `wizard/wizard-payload.ts` (validateStep/buildCreateBody/hydrate/summarise/compose
  helpers all reusable); keep mobile bottom-sheet step flow. Restore GLP-1 weekly preset, dosesPerUnit,
  category-vs-class split, per-schedule dose/label. ResponsiveSheet flips at 768px; desktop hardcodes
  sm:max-w-md — needs lg/xl width step.
- **Then #14**: knip (del `todays-dose-card.tsx` + unused `DoseUnit` export) — BUT check if overhaul
  re-uses todays-dose-card first; openapi regen; rewrite 14 e2e med-wizard specs to new flow.
- Background docs/landing agent FINISHED (findings saved). No agents currently running.
